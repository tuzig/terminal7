version: "3.9"
services:
  runner:
    build: 
      context: .
      dockerfile: ./qa/runner/Dockerfile
    volumes:
      - ./qa/peerbook_webrtc:/runner
      - ./qa/result:/result
    depends_on:
      - peerbook
      - terminal7
      - webexec
  terminal7:
    image: halverneus/static-file-server:latest
    environment:
      PORT: 80
    volumes:
      - ./dist:/web
  webexec:
    build:
      context: .
      dockerfile: ./qa/webexec/Dockerfile
    depends_on:
      - peerbook
    volumes:
      - ./qa/webexec/config:/home/webexec/.config/webexec
  peerbook:
    build:
      context: .
      dockerfile: ./qa/peerbook/Dockerfile
    expose:
      - "17777"
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis:6379
  redis:
    image: "redis:alpine"
    command: [sh, -c, "rm -f /data/dump.rdb && redis-server"]  # disable persistence

