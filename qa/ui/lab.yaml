version: "3.9"
services:
  runner:
    build: 
      context: .
      dockerfile: ./qa/runner/Dockerfile
    volumes:
      - ./qa/ui:/runner
      - ./qa/result:/result
      - ./qa/ui/webexec_conf:/webexec_config
    depends_on:
      - terminal7
      - webexec
  terminal7:
    image: halverneus/static-file-server:latest
    environment:
      PORT: 80
    volumes:
      - ./dist:/web
  webexec:
    build:
      context: .
      dockerfile: ./qa/webexec/Dockerfile
    volumes:
      - ./qa/ui/webexec_conf:/home/webexec/.config/webexec
